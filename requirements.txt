# Runtime dependencies for the AI AppSec demo.
#
# The application itself uses only the Python standard library (sqlite3), so the
# only runtime dependency is the Anthropic SDK used by the AI agent scripts.
anthropic>=0.40.0
